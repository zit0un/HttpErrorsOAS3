type: object
  required:
  - status_code
  - message
  properties:
    'status_code':
      type: integer
      enum: [403]
    'message': 
      type: string
      enum: ['The access is not allowed.']
